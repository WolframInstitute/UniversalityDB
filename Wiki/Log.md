# Activity Log

| Date | Actor | Action |
|---|---|---|
| 2026-05-01 | LLM | **Tag → CTS sorry retired.** Deleted the dead `tagToCTSSimulation` reference definition in `Lean/Proofs/TagSystemToCyclicTagSystem.lean` (its `halting` field was the only remaining sorry outside CockeMinsky). The edge wrapper `edge_TagtoCTS` already constructs the `Simulation` directly from `tagToCTSCommutes` plus the explicit `hHalt` hypothesis, so removing the dead definition is a no-op for the edge but eliminates a buried sorry. Considered but rejected option of changing `Tag.step` semantics on singletons (`[a] ↦ (productions a).tail`) — this would have matched Cook's encoding exactly but introduced a non-standard 2-tag definition; user preference was to keep the standard convention and expose the conditionality structurally. Updated `Lean/Integrity.lean` (removed `tagToCTSSimulation` from key theorems), `Lean/Edges.lean` (refreshed comments), `README.md`, `Wiki/Status.md`, `Wiki/Proofs/TagToCTS.md`, `Wiki/Proofs/TagToCTS/Skeleton.md`, `Wiki/Plans/VerificationFramework.md`. Sorry count: 2 → 1 (only `wolfram23PreservesHalting` remains). Integrity: PASS. |
| 2026-05-01 | LLM | **Verification docs clarified for direct audit.** Rewrote `README.md` so the public tables now list the main Lean statements, explicit hypotheses, template meaning, and `Sorry` counts; renamed notebook columns to `Wiki Markdown` and `Cloud Notebook`; added a short `How To Verify` checklist. Tightened `Wiki/Concepts/ProofIntegrity.md` with a direct user audit path (inspect type, inspect machine definition, run integrity script, compare with Wolfram). Updated `Wiki/Notebooks/ECASymmetries.md` so the Lean section now exposes the literal ECA rule definitions and the exact orbit theorem types; regenerated notebooks successfully with `Scripts/generate_notebooks.wls`. |
| 2026-05-01 | LLM | **ECA Klein-group consolidation.** Merged the mirror and conjugation proof surfaces into `Lean/Proofs/ElementaryCellularAutomatonKleinGroup.lean`, made the mirror simulation wrappers genuinely rule-parametric (`mirrorRuleSimulatesRule`, `ruleSimulatesMirrorRule`), removed the old shim modules, and repointed `Edges`, `Integrity`, `Status`, the ECA notebook, and the proof notes to the single file. Focused validation: `lake build Proofs.ElementaryCellularAutomatonKleinGroup Edges Integrity` passed; notebooks regenerated. |
| 2026-05-01 | LLM | **GS → TM SimulationEncoding: bridge fully discharged; `gsToTMSimulationEncoding` is sorry-free.** Resumed and finished. Per-step bridges: `decodePadded_shiftRightOne` (unconditional) and `decodePadded_shiftLeftOne` (with precondition `right.length ≥ w-1`, preserved by shiftLeft which only grows right). Key insight: shiftLeft cannot have an unconditional bridge — multi-cell shiftLeft pushes the rightmost cell (possibly a padding 0) onto the right tape, while [c]-view shiftLeft pushes the original `c`; they only agree when there is no padding. Added `decodePadded_proper_form` helper (decode without padding when right.length ≥ w-1). Per-step proofs use `List.dropLast_concat` / `List.getLastD_concat` after `List.take_succ_eq_append_getElem` rewrite. Full bridges `decodePadded_shiftByRight` / `decodePadded_shiftByLeft` proved by induction on mag, chaining via `Option.map_map`. `replAsc_eq_tail` discharged via helper `replAsc_cons_eq_take`. `gsToTMSimulationEncoding.commutes` discharged with explicit witnesses; branches on w=1 (uses `fullSim_w1`) vs w≥2 (uses `fullSim_general_cView`). Closure: `[propext, Quot.sound, Classical.choice]` (NO sorryAx). `EDGE AUDIT: PASS (10 edges)`. `INTEGRITY CHECK: PASS`. Sorry count unchanged at 2 (pre-existing in TagSystemToCTS and CockeMinsky, unrelated). |
| 2026-05-01 | LLM | **GS → TM SimulationEncoding: chain (`fullSim_general_cView`) fully discharged; bridge deferred.** Refactored `writeLoop` to k-step uniform form (no w=2 vs w≥3 split). Added `chain_left_form` + `chain_getLastD` helpers. Discharged `fullSim_general_cView` fully (0 sorry; axiom closure `[]`) — composes readScan + lastRead + writeLoop + writeZeroShift via `exactSteps_append`, lands at the [c]-view `encodeConfig (shiftBy {left, [r₀], replAsc repl (w-1) ++ right} mag dir)`. Defined `decodeConfigPadded` (pads cells with 0 to width w when right tape underflows) + static bridge `decodeConfigPadded_encodeConfig`. Updated `edge_GStoTM` to `SimulationEncoding` shape; registry's `claimShape := .simulationEncoding`. Per-step bridges (`decodePadded_shiftRightOne`, `_shiftLeftOne`) and full bridge (`decodePadded_encodeConfig_shiftBy` + `gsToTMSimulationEncoding.commutes`) stubbed with `sorry`. Stopped per max-3-attempts on tactic engineering: case analysis works on paper but `Configuration` lacks `[ext]` so `congr 1; ext1` paths get stuck. Build passes; integrity reports `fullSim_general_cView : []`. Branch: `gs-tm-attempt-2026-05-01`. See Wiki/Plans/GStoTM_SimulationEncoding.md History. |
| 2026-05-01 | LLM | **Klein-4 ECA conjugation framework + 4 new edges.** New file `Lean/Proofs/ElementaryCellularAutomatonConjugation.lean`: rule-parametric `mirrorRule`, `complementRule`, `complementTape`, `mirrorComplementTape` definitions; involutivity, commutativity; generic step-commutation theorems `complementSimulationGeneral` (no length constraint) and `mirrorSimulationGenericGeneral` (n ≥ 3) and the composed `mirrorComplementSimulation`. Specific Rule 110 orbit identifications (each `decide` over 8 neighbourhoods): `mirrorRule rule110 = rule124`, `complementRule rule110 = rule137`, `mirrorRule rule137 = rule193`, `complementRule rule124 = rule193`. Four new `Simulation` wrappers `rule110SimulatesRule137`, `rule137SimulatesRule110`, `rule110SimulatesRule193`, `rule193SimulatesRule110` with axiom closure `[propext, Quot.sound]`. Added `rule193 := ruleTable 193` to ECA Defs. Made `reverseRightIsLeft`/`reverseLeftIsRight` non-private (unchanged content). Fixed pre-existing Integrity import-sync bug: added `Edges` and `EdgeAudit` to Integrity imports. Edge registry: 6 → 10 (added `edge_ECA110_ECA137`, `edge_ECA137_ECA110`, `edge_ECA110_ECA193`, `edge_ECA193_ECA110`, all unconditional). New unified notebook `Wiki/Notebooks/ECASymmetries.md` (Klein-4 orbit calculation + visual evolution + Lean import). New wiki article `Wiki/Proofs/ECAConjugation.md`. Updated `Wiki/Systems/ElementaryCellularAutomaton.md`, `Wiki/Index.md`, `Wiki/Status.md`. Build: `EDGE AUDIT: PASS (10 edges)`, `INTEGRITY CHECK: PASS`, `ALL CHECKS PASSED`. |
| 2026-04-30 | LLM | **Refactor: `SimulationViaDecode` → `SimulationEncoding` (pure conjugation, no roundtrip, no normalize).** Replaced the two with-decode structures (dead `SimulationEncoding` with strict roundtrip; `SimulationViaDecode` with `roundtrip + normalize + commutes-modulo-normalize`) with a single clean `SimulationEncoding`: `encode + decode + commutes (conjugation) + halting`. The `commutes` field reads `step_B(b) = decode (step_A^n (encode b))` strictly. Canonicalization is now on the *machine* via `stepNormalized` (pre-existing) plus the bisimulation lemma `step_stripConfig_bisim`, not as a structure field. Retargeted `edge_TMtoGS` to `BiInfiniteTuringMachine.toComputationalMachineNormalized machine`; the existing commutes proof body became `tmToGSCommutesMoore` (Moore-step version), and the new wrapper bridges via `normalizeBiTMConfig_eq_stripConfig` (proved as a one-liner since `TuringMachineToGeneralizedShift.normalize` and `BiInfiniteTuringMachine.stripTrailingZeros` are literally the same function). `tmToGSSimulation` axiom closure: `[propext, Quot.sound, Classical.choice]` — 0 sorry. INTEGRITY CHECK: PASS. Other edges unaffected (`Simulation` and `HaltingSimulation` unchanged). Updated `Wiki/Concepts/SimulationEncoding.md` and `Wiki/Status.md`. |
| 2026-04-30 | LLM | **Cleanup: rename `TMtoGS_ViaDecode` → `TMtoGS`, drop dead Moore-Simulation code.** Renamed `Lean/Proofs/TMtoGS_ViaDecode.lean` → `Lean/Proofs/TMtoGS.lean`, namespace `TMtoGS_ViaDecode` → `TMtoGS`, theorem `tmToGSSimulationViaDecode` → `tmToGSSimulation`, edge `edge_TMtoGS_viaDecode` → `edge_TMtoGS` (`shortName` `TM_GS_viaDecode` → `TM_GS`). Deleted dead code from `TuringMachineToGeneralizedShift.lean`: `MooreStepSimulation` (structurally false on empty-tape configs), `tmToGSSimulation` (the false `Simulation`-based one), plus 7 helpers (`gsStepImpliesActive`, `gsExactStepsPrependEvaluate`, `gsHaltsAfterExactSteps`, `encodedHaltedIsGSHalted`, `mooreHaltingForward`, `mooreCommutes`, `mooreHaltingEncoded`). Updated `Lean/Integrity.lean` keyTheorems (replaced dead reference with `TMtoGS.tmToGSSimulation`) and `Lean/lakefile.lean` roots. The reusable infrastructure (`encodeBiTM`, `decodeBiTM`, `fromBiTM`, `stepCommutes`, `decodeEncode`, `normalize` lemmas, `stepCommutesNorm`) is preserved in `TuringMachineToGeneralizedShift.lean` as building blocks. Build: `EDGE AUDIT: PASS (6 edges)`, `INTEGRITY CHECK: PASS`. |
| 2026-04-30 | LLM | **Bisimulation lemma proved for BiTM**: `step_stripConfig_bisim` shows `(step c).map stripConfig = stepNormalized (stripConfig c)` — i.e., `step` is well-defined modulo trailing-zero canonicalization. Adds aux list lemmas `stripTrailingZeros_headD/_tail/_cons_congr/_idempotent/_cons_strip` and `readHead_eq` helper. Closure: only standard axioms. The analogous lemma for `GeneralizedShift` is sketched in the file (`stripConfig_idempotent` proved; `shiftRightOne_stripConfig` / `shiftLeftOne_stripConfig` / `shiftBy_stripConfig` worked out on paper but Lean formalization deferred — case analysis on cells × right tape branch is tedious mechanical work, hit Bool.and_eq_true API issues). |
| 2026-04-30 | LLM | **Foundations for unified `Simulation` template.** Added `stepNormalized`, `stripConfig`, `toComputationalMachineNormalized`, `exactStepsNormalized`, `iterationStep_normalized_eq_exactSteps` to both `Lean/Machines/GeneralizedShift/Defs.lean` and `Lean/Machines/BiInfiniteTuringMachine/Defs.lean` (additive only — no existing proof touched). These post-strip trailing zeros from both tapes, so encoded BiTM configs land in the image-preserving subset under stepping. **Migration of the 4 existing simulations to a single decode-based `Simulation` template is deferred** — Tag→CTS decode/roundtrip needs a one-hot-block parser + injectivity lemma (~30-50 lines of fresh Lean) that hit elaboration issues in this session; rolled back the partial migration to keep the project at `EDGE AUDIT: PASS (6 edges)` / `INTEGRITY CHECK: PASS`. Reconciled `Wiki/Status.md` with reality (was claiming 8 edges; actually 6 — `edge_TMtoGS` and `edge_TMtoGS_canonical` were removed in prior session but Status.md hadn't been updated). |
| 2026-04-28 | LLM | **TM→GS via decode-based commutes — commutes proof CLOSED.** Worked through with focused MCP-driven session using `lean_goal` and `lean_multi_attempt` after each tactic. The four cases (direction L/R × nextState 0/≠0) closed via: `biTMStep_eq_uniform` to clean h_step, `cases hdir` on direction, `subst hb'` on b', `iterationStep_eq_exactSteps` + `cases h_gs : GS.step` for the iteration equation, `rw [← h_gs]; unfold + simp [hne, hDecodeAct, hdir, hns, if_neg hA_ge, shiftBy, shiftRightOne/shiftLeftOne]; cases b.right/left + cases sub-tail` for the witness match. Decode equation: `unfold; simp only [if_neg/pos hA_ge2, hDecodeAct2, Option.map_some]; congr 1; rw [Configuration.mk.injEq]; refine ⟨_, _, _, _⟩` with `normalize_cons_congr` + `normalize_consHeadD_dropOne` for the tape equalities. **Final axiom closure of edge_TMtoGS_viaDecode: [propext, Quot.sound, Classical.choice] — no sorryAx, no native axioms.** EDGE AUDIT: PASS (8 edges). |
| 2026-04-28 | LLM | TM→GS via decode-based commutes: added `SimulationViaDecode` structure (encode + decode + canon + decode-based commutes + halting) to Lean/Edges.lean, plus helpers `normalizeBiTMConfig` (BiTM-side canonical form) and `decodeBiTMNormalized` (decode + normalize). Registered `edge_TMtoGS_viaDecode` as edge #8. Halting field fully proved. Commutes field is sorry'd; the audit correctly flags `axioms: [propext, sorryAx, Quot.sound]`. The framework is in place; closing commutes is ~160 lines of case analysis (direction × ns × tape emptiness), which the BiTM-side normalization absorbs naturally. EDGE AUDIT: PASS (8 edges). |
| 2026-04-28 | LLM | TM→GS canonical proof: attempted full closure of `mooreStepCanonical_step_eq`. Added helper `normalize_consHeadD_dropOne` (proved). Switched `normalizeLR` to use `TuringMachineToGeneralizedShift.normalize` so existing lemmas (`headD_normalize`, `tail_normalize`, `normalize_idempotent`, `normalize_cons_congr`) apply. Wrote the algebraic chain that closes the proof on paper: each side reduces via standard normalize lemmas. Multiple in-Lean attempts hit elaboration obstacles (Bool/Prop coercions in `by_cases`, structural pattern matching through `shiftRightOne`'s nested `match`). Reverted partial attempt; left `mooreStepCanonical_step_eq` as sorry with full algebraic content documented in Wiki/Proofs/TMtoGS/Skeleton.md. Estimated 150-250 lines of careful tactic work to close. |
| 2026-04-27 | LLM | TM→GS canonical proof structured: added `mooreStepSimulationCanonical_holds` theorem (with hk + hHeadAll preconditions) reducing the whole canonical simulation to one named private lemma `mooreStepCanonical_step_eq` (currently sorry). The lemma's structure is documented (4-case analysis + invariance helper); estimated effort ~160 lines. Build still passes; `edge_TMtoGS_canonical` wrapper closure remains clean (axioms = standard Lean only). Three tries exhausted on the proof per user policy. |
| 2026-04-27 | LLM | TM→GS novel strategy: identified that the strict-equality unconditional `MooreStepSimulation` is structurally FALSE for empty-tape configs (GS step on encoded `(s, [], h, rh::rt)` produces left=[0]; encoded next state has left=[]). Resolution: added a normalized-GS variant where step strips trailing zeros from output. New definitions in Lean/Edges.lean: `gsStepNormalized`, `gsToCMNormalized`, `encodeBiTMCanonical`, `MooreStepSimulationCanonical`. Registered `edge_TMtoGS_canonical` as the 7th edge (axioms: propext, Quot.sound; halting field fully proved; hSimCanon hypothesis is true in principle, requires case analysis to formalize). Updated Wiki/Proofs/TMtoGS/Skeleton.md with the impossibility analysis and resolution. |
| 2026-04-27 | LLM | VerificationFramework rollout: registered 6 edges in Lean/Edges.lean (ECA110↔ECA124 unconditional; TM→GS, GS→TM, Tag→CTS, CockeMinskyChain conditional with hypotheses hoisted to top-level Prop parameters). All wrapper closures sorry-free; the buried sorries in gsToTMSimulation, tagToCTSSimulation, and wolfram23PreservesHalting are excluded by either reconstructing Simulation cleanly (using existing sorry-free lemmas like gsToTMCommutes) or hoisting the missing case as a hypothesis. Added Skeleton.md per proof under Wiki/Proofs/<Name>/. EdgeAudit prints PASS (6 edges) at build time. Updated Wiki/Index.md, Wiki/Status.md. |
| 2026-04-27 | LLM | Stage 1+2 of VerificationFramework plan: created Lean/Edges.lean (named typed wrappers + edgeRegistry metadata), Lean/EdgeAudit.lean (build-time enumeration via CollectAxioms; Integrity.lean untouched per Rule 8), demonstrated on ECA Mirror (2 unconditional edges registered, axioms = standard Lean only). Wrote Wiki/Plans/VerificationFramework.md and Wiki/Proofs/ECAMirror/Skeleton.md (paper-driven layered DAG demo). Awaiting user review before rolling out to TM↔GS, Tag→CTS, CockeMinsky chain. |
| 2026-04-10 | LLM | Added proof integrity system: rules in CLAUDE.md, trust model in Wiki/Concepts/ProofIntegrity.md, Lean/Integrity.lean (CollectAxioms + leanchecker), Scripts/verify_integrity.sh, CrossValidation notebook, updated Status.md with full inventories, updated README |
| 2026-04-08 | LLM | Added agentic team member to README, created Wiki/Plans/ActionItems.md with next steps |
| 2026-04-06 | LLM | GS→TM general width: TMPhase refactor, bridge lemma proved, stepSimulation wired for all w. 1 sorry remains in read+write phase loop (fullSim_general). |
| 2026-04-06 | LLM | Updated wiki (Status, GStoTM, Moore1991) for accurate overhead/w=1 status; updated Wolfram graph edges (added Tag→CTS, fixed GS→TM τ); updated README with verified-edges table |
| 2026-04-06 | LLM | Closed last sorry in GS→TM: refactored Nat state to TMPhase inductive, proved stepSimulation_w1. Build: 0 sorry, 0 axiom. |
| 2026-04-05 | LLM | Created wiki with 22 seed articles across Systems/, Proofs/, Concepts/, Papers/ |
| 2026-04-05 | LLM | Added Plans/, Log.md, and revision workflow to CLAUDE.md |
| 2026-04-05 | LLM | Added Tour/ system: guided interactive walk with .wl code generation |
| 2026-04-05 | LLM | Renamed Wiki/Papers/ → Wiki/Resources/; moved Resources/*.md into wiki; Resources/ now on-demand (gitignored) |
| 2026-04-05 | LLM | Added Polu2020, Zheng2025, PipelineArchitecture, LeanBlueprint wiki articles |
| 2026-04-05 | LLM | Tour/ now created on demand (not pre-created) |
| 2026-04-05 | LLM | Simplified revision: wiki prose auto-maintained, revision for code/functionality only |
| 2026-04-05 | LLM | Added ## Recover sections to all resource articles; resource pipeline documented |
| 2026-04-05 | LLM | Created KnowledgeBasePlugin.md plan for extracting workflow as reusable plugin |
| 2026-04-05 | LLM | Moved Notebooks/*.md → Wiki/Notebooks/; Notebooks/ now fully generated (gitignored) |
| 2026-04-05 | LLM | Added notebook-create skill (skill 8) to plugin plan |
| 2026-04-05 | LLM | Updated all Scripts/ to read from Wiki/Notebooks/ and Wiki/Resources/ |
